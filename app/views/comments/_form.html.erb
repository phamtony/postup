<%= form_for ([@post, @post.comments.build]) do |f| %>
<p>
	<%= f.label :user %>
	<%= f.text_field :user %>
</p>

<p>
	<%= f.label :body %>
	<%= f.text_area :body, :class => "boxquote"%>
</p>

<p>
	<%= f.submit %>
</p>

<% end %>